# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Pinned to SDK 54 deliberately (not latest) to match what's currently published in the Expo Go app on the
App Store — Expo Go only ever supports one SDK version at a time, and it lags behind new `expo` npm
releases. Before bumping this project past SDK 54, confirm the Expo Go App Store release has caught up.
